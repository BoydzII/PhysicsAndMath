# PhysicsAndMath

คลังโจทย์ **ฟิสิกส์ ม.4** และ **คณิตศาสตร์ ม.ต้น** ของโรงเรียนปากช่อง จังหวัดนครราชสีมา
พัฒนาโดย **นายรังสรรค์ กรีกูล**

แต่ละวิชาเป็น **ไฟล์ HTML ไฟล์เดียวจบ** เปิดด้วยเบราว์เซอร์ได้ทันที ไม่ต้องติดตั้งอะไร
ไม่ต้องใช้เซิร์ฟเวอร์ และทำงานได้แม้ไม่มีอินเทอร์เน็ต

🔗 **[เปิดใช้งาน](https://boydzii.github.io/PhysicsAndMath/)**

---

## สองแอพในรีโพนี้

| | ฟิสิกส์ ม.4 | คณิตศาสตร์ ม.ต้น |
|---|---|---|
| โฟลเดอร์ | [`PhysicsAutoSheet/`](PhysicsAutoSheet/) | [`MathAutoSheet/`](MathAutoSheet/) |
| ไฟล์ของครู | `physics.html` | `math.html` |
| เนื้อหา | 7 บท + แนวข้อสอบเข้ามหาวิทยาลัย · 236 แม่แบบ | ม.1 ครบ 5 บท · 75 แม่แบบ |
| เชื่อม Google Sheet | ✅ มอบหมายใบงานด้วยรหัส · ส่งผลกลับอัตโนมัติ | ❌ ออฟไลน์ล้วน (เฟสถัดไป) |
| คู่มือ | [README-physics.md](PhysicsAutoSheet/README-physics.md) | [README-math.md](MathAutoSheet/README-math.md) |

ทั้งสองแอพใช้เอนจินชุดเดียวกัน แต่เป็นคนละไฟล์ คนละคลังโจทย์
และ **เก็บข้อมูลแยกกันคนละที่ในเบราว์เซอร์** จึงเปิดใช้พร้อมกันได้โดยไม่กวนกัน

## ทำอะไรได้บ้าง

- **สุ่มโจทย์ไม่ซ้ำ** — เก็บแค่ชื่อแม่แบบกับเลขสุ่ม (seed) จึงสร้างชุดเดิมกลับมาได้เป๊ะทุกตัวเลขเสมอ
- **ตรวจคำตอบทันที** พร้อม **วิธีทำละเอียด 7 ขั้น** ทุกข้อ และหัวข้อ *"กับดักของข้อนี้"*
- **โจทย์ทฤษฎีบอกด้วยว่าตัวเลือกอื่นผิดตรงไหน** ไม่ใช่แค่เฉลยข้อถูก
- **รูปประกอบสร้างจากตัวเลขในโจทย์จริง** ตัวเลขเปลี่ยน รูปเปลี่ยนตาม
- **วิเคราะห์ผู้เรียน 4 มุม** — ตามเรื่อง / ระดับความยาก / หัวข้อย่อย / ทักษะ
- **สถิติชั้นเรียน** ค่าเฉลี่ย SD การกระจายคะแนน และวิเคราะห์รายข้อ (ค่า p และ r)
- **พิมพ์ 4 แบบ** — ใบงาน · ใบเฉลย · หลักฐานการเรียนรู้ · รายงานสถิติชั้นเรียน

## ตรวจคลังโจทย์

เปิดไฟล์ต่อท้ายด้วย `?selftest=1` เช่น `math.html?selftest=1`
จะวนสร้างทุกแม่แบบ 200 ชุดตัวเลข แล้วตรวจว่าโจทย์ เฉลย และตัวเลือกไม่มีอะไรพัง

ไฟล์ตรวจงานเพิ่มเติมอยู่ใน `outputs/` — หน้ารวมรูปประกอบ และตัวอย่างโจทย์พร้อมเฉลยทุกแม่แบบ

---

## หมายเหตุสำหรับผู้ดูแล

**ที่อยู่ Google Apps Script ไม่ได้อยู่ในรีโพนี้** เพราะรีโพเป็นสาธารณะ
`PhysicsAutoSheet/physics.html` จึงมี `BUILTIN.url` เป็นค่าว่าง — ใส่ที่อยู่จริงได้ที่
แท็บ **ตั้งค่า → เชื่อมต่อ Google Sheet** (เก็บไว้ในเบราว์เซอร์ ไม่ได้เขียนกลับลงไฟล์)

**ไฟล์ฉบับขึ้นเว็บของฟิสิกส์** (`PhysicsAutoSheet/index.html`) ฝังที่อยู่ชีตไว้ข้างใน
จึงถูกกันไม่ให้ขึ้น GitHub — สร้างใหม่ทุกครั้งที่จะดีพลอย ด้วยปุ่ม
**ตั้งค่า → สร้างไฟล์สำหรับแจกนักเรียน** แล้วอัปขึ้นที่โฮสต์เดิม

`.gitignore` ของรีโพนี้ใช้วิธี **ปิดทุกอย่างก่อนแล้วเปิดเฉพาะไฟล์ที่ตั้งใจ**
เพราะโฟลเดอร์ทำงานจริงมีรายชื่อนักเรียน ตารางสอน และไฟล์สำรองคะแนนปนอยู่
จะเพิ่มไฟล์ใหม่เข้ารีโพ ต้องไปเติมบรรทัด `!` ใน `.gitignore` เท่านั้น
