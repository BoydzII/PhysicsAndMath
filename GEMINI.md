# System Instructions & Project Memory (แนวทางการทำงานร่วมกับผู้ใช้)

ไฟล์นี้กำหนดมาตรฐาน รูปแบบการทำงาน และความจำของระบบสำหรับการพัฒนาโปรเจกต์ในคลังนี้ (`PhysicsAndMath` / `ปพ69`) เพื่อให้การทำงานในครั้งต่อๆ ไปเป็นไปอย่าง **มืออาชีพ รอบคอบ มีประสิทธิภาพ และไม่ต้องอธิบายซ้ำซ้อน**

---

## 1. ข้อมูลผู้ใช้และสไตล์การทำงาน (User & Collaboration Principles)

- **ความรอบคอบและมาตรฐานระดับมืออาชีพ (Meticulous & Professional):**
  - ห้ามทำงานแบบ "ทำแค่บางข้อเป็นตัวอย่างแล้วทิ้งที่เหลือไว้" หากมีคำสั่งให้ปรับปรุงรูปแบบ จัดรูป หรือแก้บั๊ก **ต้องตรวจทานและดำเนินการให้ครบทุกข้อ ทุกบท ทุกไฟล์ที่เกี่ยวข้องทั่วทั้งโปรเจกต์เสมอ**
  - ตรวจสอบความถูกต้องของหลักฟิสิกส์ ตัวเลข หน่วย และสมการคณิตศาสตร์ทุกจุด
- **ประสิทธิภาพและการส่งมอบงานที่สมบูรณ์ (Complete Deliverables):**
  - ก่อนรายงานผลเสร็จสิ้น ให้รันการตรวจสอบ Syntax เสมอ (`node -c <files>`)
  - อัปเดต Cache Buster (`index.html`) ทุกครั้งที่มีการแก้ JS/CSS เพื่อให้เบราว์เซอร์และ iPad ของนักเรียนโหลดไฟล์เวอร์ชันล่าสุดทันที
  - ทำการ `git commit` ด้วยข้อความที่สื่อความหมายชัดเจน และ `git push` ไปยัง `origin main` ทุกครั้ง
- **การสื่อสาร (Communication):**
  - สรุปงานเป็นภาษาไทยที่กระชับ ชัดเจน มีโครงสร้าง เข้าประเด็นตรงไปตรงมา
  - แนบลิงก์ไปยังไฟล์ที่ถูกแก้ไขเสมอ

---

## 2. โครงสร้างโปรเจกต์หลักใน Workspace

1. **Physics2App/** (เว็บแอปพลิเคชันเรียนรู้ฟิสิกส์ ม.4-ม.6):
   - สมุดบันทึกดิจิทัล (Digital Notebook Paper) รองรับการเขียน/วาดด้วย Apple Pencil / Touch บน iPad
   - ระบบโจทย์ 3 ระดับความยาก: Beginner (แสดงไกด์วิธีทำ), Intermediate (แสดงข้อมูลโจทย์+ทดในกระดาษ), Advanced (โจทย์เพียว+พื้นที่ทด)
   - โมเดลจำลอง Interactive Physics Simulations บน HTML5 Canvas
   - คลังข้อมูลโจทย์และเนื้อหา:
     - `data.js`: บทที่ 5 งานและพลังงาน (5.1 งาน, 5.2 กำลัง, 5.3 พลังงานจลน์, 5.4 พลังงานศักย์, 5.5 กฎการอนุรักษ์พลังงาน)
     - `data_ch6.js`: บทที่ 6 โมเมนตัมและการชน (6.1 โมเมนตัม, 6.2 แรงและการดล, 6.3 การชน)
     - `data_ch7.js`: บทที่ 7 การเคลื่อนที่แบบต่างๆ (7.1 โพรเจกไทล์, 7.2 วงกลม, 7.3 ฮาร์มอนิกอย่างง่าย)
     - `data_ch5_challenge.js`, `data_ch6_challenge.js`, `data_ch7_challenge.js`: แบบฝึกหัดท้าทายประจำบท
2. **AutoSheets (ปพ.5 / ปพ.69):**
   - `PhysicalScienceAutoSheet`, `PhysicsAutoSheet`, `ChemistryAutoSheet`, `BiologyAutoSheet`, `MathAutoSheet`
   - ระบบตัดเกรด บันทึกคะแนนเก็บ และประเมินผลตามตัวชี้วัด สพฐ. (หลักสูตรแกนกลาง)

---

## 3. มาตรฐานการจัดรูปสมการในโจทย์ (Physics2App Equation Standards)

### กฎเหล็กของการแสดงวิธีทำ (.calc-steps)
1. **ห้ามเขียนเครื่องหมายเท่ากับต่อกันในแนวนอนยาวๆ** (เช่น `$W = Fs = 500 	imes 10 = [5000]	ext{ J}$` เป็นสิ่งต้องห้าม)
2. **เครื่องหมายเท่ากับ ($=$) ต้องขึ้นบรรทัดใหม่ และต้องจัดตำแหน่งให้ตรงกันในแนวดิ่งเสมอ**
3. **ใช้คลาส CSS Grid `.calc-steps` เสมอ** ซึ่งมี 3 คอลัมน์ (`auto auto 1fr`):
   - คอลัมน์ 1: ตัวแปร/ชื่อขั้นตอน (เช่น `<div>2. $v$</div>` หรือบรรทัดถัดไปเป็น `<div></div>` ว่างไว้)
   - คอลัมน์ 2: เครื่องหมายความสัมพันธ์ (เช่น `<div>$=$</div>` หรือ `<div>$\approx$</div>`)
   - คอลัมน์ 3: นิพจน์/การคำนวณ/ช่องกรอกคำตอบ (เช่น `<div>$\sqrt{2gh}$</div>` หรือ `<div><input type="text" class="answer-input" placeholder="10">&nbsp;m/s</div>`)
4. **ความลึกของขั้นตอนการคำนวณ:** นิยมจัดเป็น 4-5 บรรทัดตามหลักการสอน:
   - บรรทัดที่ 1: แสดงสูตรฟิสิกส์หลัก
   - บรรทัดที่ 2: แทนค่าตัวแปรลงในสูตร
   - บรรทัดที่ 3: ขั้นตอนการจัดรูปหรือคิดเลขขั้นกลาง
   - บรรทัดที่ 4/5: คำตอบสุดท้าย พร้อมช่อง `<input class="answer-input">` และหน่วย

### กฎการเขียน LaTeX และ KaTeX
- **การ Escape ใน JavaScript String:** ต้องใช้ Double Backslash เสมอ เช่น `\\frac`, `\\times`, `\\sqrt`, `\\cos`, `\\theta`, `\\Delta`, `\\approx`
- **การเขียนหน่วย:** ห้ามเอา `\cdot` ใส่ใน `\text{}` (เช่น `\text{kg\cdot m/s}` ผิด) ให้เขียนแยกเป็น `\text{kg}\cdot\text{m/s}` หรือ `\text{N}\cdot\text{s}`
- **ห้ามมี Stray `$` ใน Display Math:** ในบล็อก `$$\begin{aligned} ... \end{aligned}$$` ห้ามมีเครื่องหมาย `$` อยู่ข้างในเด็ดขาด

---

## 4. มาตรฐานระบบจำลอง Interactive Simulation

1. **รองรับสัมผัสและเมาส์ (Touch & Mouse Support):** iPad Friendly (TouchEvent + MouseEvent หรือ PointerEvent)
2. **เน้นภาพชัดเจนเชิงการสอน (Pedagogical Visuals):**
   - รูปทรงและสีสันสื่อความหมายชัดเจน (เวกเตอร์แรง, ทิศทางการเคลื่อนที่, กราฟแท่งพลังงาน)
   - ไม่เน้นความสมจริงที่ซับซ้อนเกินไปจนบดบังหลักการฟิสิกส์ (ตามความต้องการของผู้ใช้: "เมื่อลากแล้วเห็นภาพชัดเจนมากกว่าสมจริงเกินไป")
3. **Smooth Animation:** ใช้ `requestAnimationFrame` และคำนวณพิกัดด้วย `canvas.getBoundingClientRect()` เสมอ

---

## 5. เช็คลิสต์ก่อนส่งงาน (Definition of Done)

- [ ] ทุกข้อที่อยู่ในขอบเขตได้รับการแก้ไขครบ 100% (ไม่มีข้อที่ตกหล่นหรือค้างไว้)
- [ ] ตรวจสอบ KaTeX ไม่ error และหน่วยถูกต้อง
- [ ] รัน Syntax validation ผ่าน 100%: `node -c <files>`
- [ ] รัน Cache Buster อัปเดต timestamp ใน `Physics2App/index.html`
- [ ] Git commit และ push ขึ้น `origin main` สำเร็จ
