# System Instructions & Project Memory (แนวทางการทำงานร่วมกับผู้ใช้)

ไฟล์นี้กำหนดมาตรฐาน รูปแบบการทำงาน และความจำของระบบสำหรับการพัฒนาโปรเจกต์ในคลังนี้ (`PhysicsAndMath` / `ปพ69`) เพื่อให้การทำงานในครั้งต่อๆ ไปเป็นไปอย่าง **มืออาชีพ รอบคอบ มีประสิทธิภาพ และไม่ต้องอธิบายซ้ำซ้อน**

---

## 1. ข้อมูลผู้ใช้และสไตล์การทำงาน (User & Collaboration Principles)

- **ความรอบคอบและมาตรฐานระดับมืออาชีพ (Meticulous & Professional):**
  - ห้ามทำงานแบบ "ทำแค่บางข้อเป็นตัวอย่างแล้วทิ้งที่เหลือไว้" หากมีคำสั่งให้ปรับปรุงรูปแบบ จัดรูป หรือแก้บั๊ก **ต้องตรวจทานและดำเนินการให้ครบทุกข้อ ทุกบท ทุกไฟล์ที่เกี่ยวข้องทั่วทั้งโปรเจกต์เสมอ 100%**
  - ตรวจสอบความถูกต้องของหลักฟิสิกส์ ตัวเลข หน่วย และสมการคณิตศาสตร์ทุกจุด
- **ประสิทธิภาพและการส่งมอบงานที่สมบูรณ์ (Complete Deliverables):**
  - ก่อนรายงานผลเสร็จสิ้น ให้รันการตรวจสอบ Syntax เสมอ (`node -c <files>`)
  - รันสคริปต์ตรวจสอบสมการอัตโนมัติ: `python .agents/skills/physics-equation-formatter/scripts/validate_equations.py`
  - อัปเดต Cache Buster (`index.html`) ทุกครั้งที่มีการแก้ JS/CSS เพื่อให้เบราว์เซอร์และ iPad ของนักเรียนโหลดไฟล์เวอร์ชันล่าสุดทันที
  - ทำการ `git commit` ด้วยข้อความที่สื่อความหมายชัดเจน และ `git push` ไปยัง `origin main` ทุกครั้ง
- **การสื่อสาร (Communication):**
  - สรุปงานเป็นภาษาไทยที่กระชับ ชัดเจน มีโครงสร้าง เข้าประเด็นตรงไปตรงมา
  - แนบลิงก์ไปยังไฟล์ที่ถูกแก้ไขเสมอ

---

## 2. คลังสกิลเฉพาะทางใน Workspace (.agents/skills/)

ระบบมีชุดสกิลเฉพาะทาง 5 ด้านที่ติดตั้งไว้พร้อมใช้งาน:
1. **`physics-equation-formatter`:** มาตรฐานการสร้างสมการฟิสิกส์-คณิตศาสตร์, เวกเตอร์หนึ่งหน่วย $\hat{i}, \hat{j}, \hat{k}$, การจัดรูป `.calc-steps` 3 คอลัมน์, และตัวตรวจสอบอัตโนมัติ
2. **`physics-simulation-builder`:** สถาปัตยกรรม Canvas Interactive Simulations สำหรับสอนฟิสิกส์ (Touch + Mouse, ภาพชัดเจนเชิงมโนทัศน์, เวกเตอร์, แท่งพลังงาน)
3. **`goodnotes-digital-ink`:** สมุดบันทึกดิจิทัลมีเส้นบรรทัด, การปฏิเสธฝ่ามือ (Palm Rejection), ปากกาเน้นข้อความแบบ Multiply, การซูมแบบ Pinch-to-Zoom
4. **`student-assignment-submission`:** ระบบส่งงานของนักเรียน (พิมพ์ PDF พร้อมลายมือ Apple Pencil, ตรวจคำตอบอัตโนมัติ, ส่งคะแนนเข้า Google Sheets / ปพ.5)
5. **`autosheet-thai-curriculum`:** การตัดเกรด บันทึกคะแนนเก็บ และประเมินผลตามตัวชี้วัด สพฐ. (ปพ.5 / ปพ.69)
6. **`physics-interactive-workbook`:** การสร้างและดูแลแบบฝึกหัดฟิสิกส์แบบโต้ตอบ การจัดโครงสร้างความยาก 3 ระดับ การซูมเฉพาะหน้ากระดาษ (Sheet-Only Zoom) และการประสานงานเลเยอร์ลายมือ/DOM

---

## 3. มาตรฐานการจัดรูปสมการในโจทย์ (Equation Standards)

### กฎเหล็กของการแสดงวิธีทำ (.calc-steps)
1. **ห้ามเขียนเครื่องหมายเท่ากับต่อกันในแนวนอนยาวๆ** (เช่น `$W = Fs = 500 \times 10 = [5000]\text{ J}$` เป็นสิ่งต้องห้าม)
2. **เครื่องหมายเท่ากับ ($=$) ต้องขึ้นบรรทัดใหม่ และต้องจัดตำแหน่งให้ตรงกันในแนวดิ่งเสมอ**
3. **ใช้คลาส CSS Grid `.calc-steps` เสมอ** ซึ่งมี 3 คอลัมน์ (`auto auto 1fr`):
   - คอลัมน์ 1: ตัวแปร/ชื่อขั้นตอน (เช่น `<div>2. $v$</div>` หรือบรรทัดถัดไปเป็น `<div></div>` ว่างไว้)
   - คอลัมน์ 2: เครื่องหมายความสัมพันธ์ (เช่น `<div>$=$</div>` หรือ `<div>$\approx$</div>`)
   - คอลัมน์ 3: นิพจน์/การคำนวณ/ช่องกรอกคำตอบ (เช่น `<div>$\sqrt{2gh}$</div>` หรือ `<div><input type="text" class="answer-input" placeholder="10">&nbsp;m/s</div>`)
4. **สัญลักษณ์เวกเตอร์และเวกเตอร์หนึ่งหน่วย:**
   - เวกเตอร์: $\vec{F}, \vec{v}, \vec{p}, \vec{r}$
   - ยูนิตเวกเตอร์: $\hat{i}, \hat{j}, \hat{k}$ (เขียนด้วย `\hat{i}, \hat{j}, \hat{k}`)
   - ดอตโพรดักต์: `\vec{A} \cdot \vec{B}` (ใช้ `\cdot`)
   - ครอสโพรดักต์: `\vec{A} \times \vec{B}` (ใช้ `\times`)
5. **ความลึกของขั้นตอนการคำนวณ:** จัดเป็น 4-5 บรรทัดตามหลักการสอน:
   - บรรทัดที่ 1: แสดงสูตรฟิสิกส์หลัก
   - บรรทัดที่ 2: แทนค่าตัวแปรลงในสูตร
   - บรรทัดที่ 3: ขั้นตอนการจัดรูปหรือคิดเลขขั้นกลาง
   - บรรทัดที่ 4/5: คำตอบสุดท้าย พร้อมช่อง `<input class="answer-input">` และหน่วย

---

## 4. เช็คลิสต์ก่อนส่งงาน (Definition of Done)

- [ ] ทุกข้อที่อยู่ในขอบเขตได้รับการแก้ไขครบ 100% (ไม่มีข้อที่ตกหล่นหรือค้างไว้)
- [ ] รันการตรวจสอบสมการ: `python .agents/skills/physics-equation-formatter/scripts/validate_equations.py` ผ่าน 100%
- [ ] รัน Syntax validation ผ่าน 100%: `node -c <files>`
- [ ] รัน Cache Buster อัปเดต timestamp ใน `Physics2App/index.html`
- [ ] Git commit และ push ขึ้น `origin main` สำเร็จ
